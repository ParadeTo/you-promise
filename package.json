{
  "name": "you-promise",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --env=node --verbose --color --watch",
    "test:single": "jest --env=node --verbose --color"
  },
  "author": "",
  "license": "ISC",
  "standard": {
    "globals": [
      "it",
      "describe",
      "test",
      "expect",
      "beforeEach",
      "jest",
      "Response"
    ]
  },
  "devDependencies": {
    "jest": "^23.4.2"
  }
}
